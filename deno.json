{
  "tasks": {
    "test": "deno test --doc --parallel --shuffle --allow-net=deno.land,localhost,0.0.0.0 --allow-read=.",
    "check": "deno check ./mod.ts",
    "dev": "deno run -A  example/serve.ts"
  },
  "importMap": "./import-map.json"
}
